import classNames from 'classnames';

const withCaptionLayoutClass = classNames('flex flex-col gap-y-2 md:gap-y-4');
const captionClass = classNames('text-xs text-gray-500 dark:text-gray-400');

export { withCaptionLayoutClass, captionClass };
